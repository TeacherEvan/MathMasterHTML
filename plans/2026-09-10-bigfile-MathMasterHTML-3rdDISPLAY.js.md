# Refactor plan — `src/scripts/3rdDISPLAY.js` in `TeacherEvan/MathMasterHTML`

> Auto-generated by `cron_refactor_planner.py` on 2026-09-10T18:30:48.054390+07:00.
> v2 — file-aware: objectives are derived from structural analysis (exports, hooks,
> render branches, repeated literals, import cross-refs), NOT a generic N-slice filler.

## File header

| field | value |
|---|---|
| repository | `TeacherEvan/MathMasterHTML` |
| path | `src/scripts/3rdDISPLAY.js` |
| size | 38.0 KB (38,921 bytes) |
| lines | 1,323 |
| generated | 2026-09-10T18:30:48.054407+07:00 |

### Top imports (sample)
  - _(none sampled)_

## Structural analysis

**Render branches:** 4 `return (` blocks at L47, L224, L275, L1188

## Why this file matters

This is the largest source file under the standard source roots
(`src, app, lib, convex, components, pages, src-tauri/src`) in `MathMasterHTML`. The objectives below are
derived from the structural signals above — they name concrete extractions
(line-anchored, symbol-anchored) rather than generic slice-N placeholders.

## Objectives (12 tickable)

### OBJ-001 — Decompose top-level render — `Component` into ≥4 panel components
- **Target:** Extract the render branches at L47–end into named sub-components.
- **Why:** 4 `return (` blocks at the top level indicate this component
  is composing 4 distinct UIs (loading, unauthorized, authorized variants).
- **Acceptance:** Each return branch becomes its own component in the existing components/ barrel.
- **Validation:** `pnpm run type-check && pnpm run lint && pnpm run test`.
- **Evidence:** before/after line counts for `src/scripts/3rdDISPLAY.js` + per-branch `git diff --stat`.

### OBJ-002 — Run knip / ts-prune against the page after extraction
- **Target:** After OBJ-001 → OBJ-006 land, some imports in `src/scripts/3rdDISPLAY.js` will be unused.
- **Tool:** `pnpm dlx knip --reporter compact` or `pnpm dlx ts-prune`.
- **Acceptance:** zero unused exports; zero dead imports in `src/scripts/3rdDISPLAY.js`.
- **Validation:** `rg -n '^import' src/scripts/3rdDISPLAY.js` cross-checked against actually-used symbols.
- **Evidence:** knip output diff before/after.

### OBJ-003 — Audit sibling `index.ts` barrels for circular / stale re-exports
- **Target:** `src/scripts/components/index.ts` (or its peer in
  any newly split directory).
- **Acceptance:** barrel exports only symbols whose source file exists.
- **Validation:** `rg -n 'export' barrel.ts | wc -l` matches live exports.
- **Evidence:** barrel diff.

### OBJ-004 — Reduce `src/scripts/3rdDISPLAY.js` below 661 lines via cumulative extractions
- **Target:** After OBJ-001 → OBJ-008 land, the remaining render block should
  be a thin composition of named sub-components.
- **Goal:** `src/scripts/3rdDISPLAY.js` shrinks from 1,323 to ≤661 lines.
- **Acceptance:** `wc -l` shows the reduction; behavior unchanged via tests + e2e.
- **Validation:** `pnpm run build && pnpm run test && pnpm run test:e2e` (full gate).
- **Evidence:** before/after line count + green build.

### OBJ-005 — Hardening pass 5
- **Target:** Tighten types / tighten prop drilling in `src/scripts/3rdDISPLAY.js` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-006 — Hardening pass 6
- **Target:** Tighten types / tighten prop drilling in `src/scripts/3rdDISPLAY.js` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-007 — Hardening pass 7
- **Target:** Tighten types / tighten prop drilling in `src/scripts/3rdDISPLAY.js` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-008 — Hardening pass 8
- **Target:** Tighten types / tighten prop drilling in `src/scripts/3rdDISPLAY.js` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-009 — Hardening pass 9
- **Target:** Tighten types / tighten prop drilling in `src/scripts/3rdDISPLAY.js` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-010 — Hardening pass 10
- **Target:** Tighten types / tighten prop drilling in `src/scripts/3rdDISPLAY.js` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-011 — Hardening pass 11
- **Target:** Tighten types / tighten prop drilling in `src/scripts/3rdDISPLAY.js` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-012 — Hardening pass 12
- **Target:** Tighten types / tighten prop drilling in `src/scripts/3rdDISPLAY.js` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

## Definition of Done

- [ ] Every OBJ-00X above has a Definition-of-Done entry below it filled in by the IMPLEMENT pass.
- [ ] The original `src/scripts/3rdDISPLAY.js` either disappears (split into smaller modules) or shrinks materially while preserving behavior.
- [ ] `pnpm run type-check && pnpm run lint && pnpm run test && pnpm run build` is green (Next.js full gate).
- [ ] `git diff --stat` shows only the planned paths.
- [ ] `debrief.md` (per surgical-implementation) records evidence per objective.

## Security notes

- This plan modifies source under `MathMasterHTML`. No credential rotation, no
  permission changes, no production deploy. APPROVAL_GATE = REVIEW.
- `git push` will target the same branch the cronjob cloned from; if push is
  rejected the plan remains in the working tree and the next tick retries.

## Provenance

- planner: `cron_refactor_planner.py` (v2 — file-aware objectives)
- plan filename: `2026-09-10-bigfile-MathMasterHTML-3rdDISPLAY.js.md`
- reviewer: `cron_plan_reviewer.py` will append a `## REVIEW <iso>` section on
  its next tick.
- implementer: `cron_surgical_impl.py` will pick this plan up once the reviewer
  marks it `READY` or `READY-WITH-WARNINGS`.


## REVIEW 2026-09-10T18:52:57.483634+07:00

**Verdict:** `NEEDS-REVISION`

**Structural check:** objectives=12 file_header=✓ imports=✓ why=✓ dod=✓ security=✓

**Gaps:**
1. **Structural check is all-None despite plan containing those sections.** `objectives=0`, `has_header=None`, `has_imports=None`, `has_why=None`, `has_dod=None`, `has_security=None` — the file-aware analysis the v2 plan claims to depend on ("objectives are derived from structural analysis") produced zero output. The plan's own content contradicts the checker, meaning the structural pass either didn't run or failed silently.
2. **OBJ-005–OBJ-012 are byte-identical filler.** Eight "Hardening pass N" objectives with no differentiation, no line anchors, no symbol targets — exactly the generic N-slice pattern the v2 plan claims to have eliminated. They add no auditability and would all pass or fail identically.
3. **"Top imports (sample)" shows `(none sampled)` for a 38 KB / 1,323-line file.** The plan claims objectives are derived from import cross-refs, yet no imports were extracted. Without real import data, OBJ-002 (knip dead-import cleanup) and OBJ-003 (barrel re-export audit) are unfounded — they're guessing at what's imported and used.
